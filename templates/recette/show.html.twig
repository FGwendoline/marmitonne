{% extends "base.html.twig" %}

{% block body %}

    <div class="container">

        <div class="jumbotron">
            <h2>{{ recette.titre }}</h2>
        </div>
        <div class="row">
            <div class="col-md-3">{{ recette.temps }}</div>
            <div class="col-md-3">{{ recette.personnes }}</div>
            </div>
        <div class="row">
            <div class="col">
                {{ recette.preparation }}
            </div>
        </div>

    </div>

{% endblock %}